#common settings for topology
[settings]
stack_name = stack1
esx_host = 172.18.93.32
esx_user = root
esx_password = vmware

networks = net1, net2, net3
vms = vm_test1, vm_test2

# specific settings for some host
# memory, cpu, disk_space - non-default parameters
[vm_test1]
networks = net1, net2
description = Virtual machine 1
vnc_port = 5900
login = vyatta
password = vyatta
iso = /vyatta.iso

configuration = set int eth eth0 address 10.10.10.1/25,
                set int eth eth1 address 100.10.10.1/25,
                commit,save,exit

[vm_test2]
networks = net2, net3
description = "Virtual machine 2"
memory = 512
cpu = 2
disk_space = 1024
iso = /vyatta.iso
vnc_port = 5901
configuration = set int eth eth0 address 10.10.10.2/25,
                set int eth eth1 address 100.10.10.2/25

[net1]
num_ports = 5
promiscuous = True
vlan = 1024

[net2]
num_ports = 7
promiscuous = True
vlan = all

[net3]
num_ports = 1024
promiscuous = false
vlan = all